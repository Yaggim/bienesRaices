extend ../layout/index

block contenido
    div(class="py-10")
        h1(class="text-4xl my-10 text-center font-extrabold") Bienes 
            span(class="font-normal") Raices
        h2.text-center.text-2xl= pagina

        if errores
            div(class="max-w-md mx-auto my-10")
                each error in errores
                    p(class="bg-red-600 text-white uppercase text-xs text-center p-2 mb-1 font-bold")= error.msg

        div(class="mt-10 mx-auto max-w-md")
            div(class="bg-white py-8 px-4 shadow")
                form(class="space-y-5" method="POST" noValidate)
                    input(type="hidden" name="_csrf" value=csrfToken)
                    ddiv 
                        label(class="block text-sm uppercase text-gray-500 font-bold mb-2" for="password") Contraseña
                        input(id="password" class="w-full px-3 py-2 border border-gray-300 rounded-md placeholder-gray-400" 
                        placeholder="Tu contraseña" type="password" name="password")
                    div 
                        label(class="block text-sm uppercase text-gray-500 font-bold mb-2" for="repetir_password") Repetir contraseña
                        input(id="repetir_password" class="w-full px-3 py-2 border border-gray-300 rounded-md placeholder-gray-400" 
                        placeholder="Repetir contraseña" type="password" name="repetir_password")
                    div(class="flex items-center justify-between")
                        a(href="/auth/registro" class="text-sm text-indigo-500 hover:text-indigo-700") ¿No tenes cuenta? Registrate
                        a(href="/auth/login" class="text-sm text-indigo-500 hover:text-indigo-700") ¿Ya tienes cuenta? Inicia sesión
                    input(type="submit" value="Cambiar contraseña" 
                    class="bg-indigo-600 hover:bg-indigo-700 w-full text-white font-bold py-3 cursor-pointer")