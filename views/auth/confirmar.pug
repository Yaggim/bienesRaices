extend ../layout/index

block contenido
    div(class="py-10")
        h1(class="text-4xl my-10 text-center font-extrabold") Bienes 
            span(class="font-normal") Raices
        h2.text-center.text-2xl= pagina

        div(class="max-w-md mx-auto my-10 bg-white py-8 px-4 shadow rounded-lg")
            p(class= error ? 'bg-red-500' : 'bg-green-500', class=' text-center text-xl font-bold text-white my-10 rounded-lg py-4')=mensaje

            if !error
                div(class="flex justify-center max-w-md mx-auto pb-2")
                    a(href="/auth/login" class="text-center text-xl font-bold text-indigo-500 hover:text-indigo-700") Inicia sesión
        